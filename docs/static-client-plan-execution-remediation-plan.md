# Static-client plan execution remediation plan

This plan incorporates the adversarial review of the failed fresh-agent acceptance.

1. Add one shared accepted-plan payload renderer in the plan engine. Generate preferred
   and stable-client calls from the immutable accepted snapshot, validate the one-child
   shape, and include separately conditioned paired-action fallbacks.
2. Use that renderer for ordinary generated plan tools, setup plans, and the custom approved
   recovery path. Keep NULL, rejection, and recovery-disclosure responses non-executable.
3. Update the handshake, plan NULL guides, plan index descriptions, `action_batch` index
   description, agent contract, README, and architecture documentation. Preserve dynamic
   discovery as preferred and narrowly describe the exact static-client fallback.
4. Route every matching profile name to `board_validate`. Keep validation responses as the
   authority for choosing repair, safety, attachment, mismatch, or retry remedies.
5. Add focused tests for payload canonicality, paired setup behavior, permissions, budget
   consumption, stale/replaced plans, recursion/shape attacks, static stdio clients, direct
   parity, and validation-first routing. Rebaseline versioned contracts intentionally.
6. Run focused tests, Ruff, Pyright, the complete suite, packaging/import checks, and bounded
   stdio boot/EOF smoke.
7. Start a new autonomous agent with a new empty sibling repository and only the product
   goal. Answer only its ordinary user questions. Require it to finish setup before writing
   code, then independently create, build, flash, and verify the Zephyr application on real
   hardware. If it fails, record the next concrete gap and repeat this loop.

## Completion

All implementation and software-validation steps passed. The repeated fresh
agent acceptance also passed setup, readiness, autonomous code creation,
application-only flash, and state-preserving UART ON/status/OFF/status proof.
See `docs/evidence/autonomous-static-client-acceptance-2026-07-17.md`.
