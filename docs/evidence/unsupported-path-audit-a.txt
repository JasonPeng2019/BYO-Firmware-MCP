- **HIGH — `src/pyocd_debug_mcp/server.py:575-585`**: Guarded symbol writes perform memory-map containment before the new function/alignment refusal. `_symbol_scalar_refusal()` runs only later in `tools/memory.py:586`, after `PlanEngine.enforce()` has run preconditions and debited the plan (`plan_engine.py:907-955`). Thus a normal Thumb `STT_FUNC` in flash returns a containment refusal instead of `memory/symbol-is-function`; if a function resides in writable RAM, the handler returns the intended refusal but consumes the one-call plan. This violates `docs/function-symbol-memory-refusal-spec.md`’s requirement to refuse before containment. **Smallest general fix:** centralize scalar-symbol validation and invoke it in `_enforce_action_containment` immediately after `resolve_symbol()` and before `check_memory_write`/storing the prepared symbol; preserve its stable refusal code. Add a full guarded-dispatch test for function and unaligned symbols asserting no containment call and no plan consumption.