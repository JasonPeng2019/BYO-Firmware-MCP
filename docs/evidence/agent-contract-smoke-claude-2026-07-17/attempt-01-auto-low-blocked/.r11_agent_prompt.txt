# Board-free MCP interaction-contract smoke

You are the agent under test. Complete this single bounded scenario using only the `pyocd-debug` MCP server tools it advertises. Do not use hardware, a probe, UART, setup execution, validation execution, safety execution, connect, or any real board action.

1. Call `initialization_handshake` once and read its returned guidance.
2. Treat this as the user's literal conversational answer: **no board**. Call `setup_overview` with the literal one-element board-name list containing `no board`.
3. Read its returned status. If it is `setup_no_board`, stop all hardware-path, setup, and validation work. Do not try another spelling, create a board profile, or call a routed setup action.
4. The user now asks only: "What would setup require later for a hypothetical new board?" To learn the contract, call the advertised `board_setup-plan` exactly once with every one of its top-level plan fields set to null. Read the returned NULL-plan teaching. Do not submit a populated plan. Do not call `board_setup`, `board_fix_setup`, `board_validate`, any safety tool, any connection tool, or any hardware-facing action.
5. Give the user a short ordinary-English explanation of what future setup would require. In that user-facing prose, do not expose JSON, plan objects, internal IDs, board IDs, connection IDs, continuation tokens, permission enums, or invented parameter names. Do not claim setup or validation ran.

Only request MCP tools present in the server's advertised tool list. A built-in file-write tool may be used solely for the harness result file; it is not a server action.

After completing the scenario, write one JSON object to the absolute result path supplied by the command-adapter contract. It must have exactly these fields:
- `scenario_status`: `completed`, `failed`, or `blocked`
- `setup_status`: the exact setup status string observed, or null
- `stopped_hardware_path`: boolean
- `null_plan_read_only`: boolean
- `populated_plan_submitted`: boolean
- `real_setup_validation_or_hardware_called`: boolean
- `user_facing_response`: the same short ordinary-English response you give the user
- `notes`: an array of short strings

Do not perform extra scenarios or retries.

Command-adapter result contract:
Write the exact structured result JSON object to `C:\Users\Jason\Documents\Jason\FirmCLI\BYO-Server\docs\evidence\agent-contract-smoke-claude-2026-07-17\.r11_agent_result.json` before exiting.
