You are Subagent A, running with GPT 5.6 terra at medium effort in a new empty project repository. Complete the entire real-hardware journey yourself through the connected pyocd-debug MCP server. Work only in this project repo; do not edit the MCP server source.

Starting state and user facts:
- The only technical reference in the project is Nano_BLE_MCU-nRF52840_PS_v1.1.pdf. Read it as needed.
- Two physical boards are attached. Use the server inventory and friendly routing to name/assign both: "Nordic LED acceptance board" is the nRF/J-Link attachment you will develop; "STM32 companion board (unused)" is the other attachment and must not be mutated.
- The user authorizes application flashing of the Nordic LED acceptance board only. This does NOT authorize target_unlock, mass erase, bootloader flashing, or any other destructive recovery. Conversation is not plan permission: follow the exact plan response and put any permission only in the plan parameter when the plan contract requires it.
- UART is required at 115200 baud.

Hard build mechanism rules:
- Use Zephyr OS for the application.
- Use ONLY the GENERAL build helper and generalized mechanisms returned by get_setup_status.build_guidance. Use its exact argv template with the project path, a new/empty build directory, and the correct project-native board target.
- DO NOT invoke python -m pyocd_debug_mcp.zephyr_build, pyocd-zephyr-build, any Zephyr-specific helper, or any Zephyr-specific backup mechanism.
- DO NOT download or bootstrap Zephyr, NCS, an SDK, a toolchain, packs, or dependencies. The server/general helper must select a complete local environment. If guidance is Zephyr-helper-specific, if the local environment is missing, or if any command attempts a download, STOP immediately and report the defect without substituting another mechanism.
- Build exactly once. Do not rebuild unless a source defect is proven and you first record diagnosis and a concrete fix plan.

Required workflow:
1. Read initialization_handshake, then use setup_overview. Route both attached connections one-to-one by server-provided friendly choices, but perform setup/validation only for the Nordic board. Follow setup tools exactly, including all-NULL board_setup-plan first if setup is needed. Use the datasheet path in this repo, observed exact MCU identity, the nRF/J-Link attachment, UART required=true, and the stable UART identity selected by the server. Never call target_unlock or any erase/bootloader action.
2. Validate the Nordic board and call get_setup_status. Confirm ready_for_code and ready_for_uart_work. Save the exact build_guidance in your report. It must name pyocd_debug_mcp.native_build/general native build, not the Zephyr-specific helper.
3. Author a complete Zephyr application in this repo. It must have two genuine Zephyr threads: one LED blink thread and one UART console thread. Protect shared blink state safely. The console must accept at least: `blink on`, `blink off`, `blink status`, and `blink rate <milliseconds>` (or equally clear period command). Commands must actually change behavior, not merely echo. Each LED toggle must emit a timestampable/observable UART print. Blinking must continue while console input/output is serviced.
4. Build once with the exact general-helper command from build_guidance. Use the appropriate native target for the attached Nordic board. Preserve the full command/output and confirm it selected local NCS files and made no download attempt. Do not run the legacy helper.
5. Use collect_build_artifacts with the exact ELF and matching linker map from the successful build, into a new/empty collection directory, expected_roles ["elf","map"]. Record hashes/result.
6. Flash only through the guarded application path: call flash_application-plan with every parameter NULL first; then submit exactly the populated plan shape it teaches, with permission only as the plan response requires; then call flash_application on the plan-bound canonical artifact. Never unlock, mass erase, erase manually, flash a bootloader, or call a backend outside MCP.
7. Verify over UART through the planned serial_exchange path. Exercise every command, demonstrate on/off/status/rate behavior, capture toggle prints, and show toggles continue around concurrent command handling. If this verification fails because of your application, diagnose root cause, write a fix plan, change the minimum source, rebuild only then, and repeat the guarded flash and UART test. Never wave through a failure.
8. Write `journey-report.md` with: exact provider/model/effort; CLI version if visible; MCP timeline; board_id/connection/probe/UART stable IDs; plan IDs; source design; exact build command and its JSON evidence; proof local NCS was used and no download was attempted; artifact paths/hashes; flash result; full UART transcript; multithreading evidence; any failure-loop diagnosis/plan/change/retest; and final board state. Finish with a concise success/failure report. Do not fabricate anything.
