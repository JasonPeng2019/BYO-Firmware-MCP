# Plan: unsupported-path audit remediation

1. Move scalar-symbol preflight into guarded write containment and add no-consumption tests.
2. Classify inventory UART endpoints from stable probe association and exercise external-adapter
   confirmation in production-shaped tests.
3. Add a replayable built-in-target support record and deterministic target-class geometry adapter;
   extend profile and generic map authority unions without changing pack semantics.
4. Accept verified built-in targets in target research, capture live CPUID compatibility evidence,
   persist the resulting authority, and test restart/replay/map creation.
5. Add a typed debug protocol field through board config, target-control, and pyOCD. Live-test target
   plus attachment policy during continuation; stage state locally and publish only a successful
   pair. Remove the unreachable post-consumption attachment-research route.
6. Validate Arm CPUID architectural fields, add generic Cortex-M system-control geometry, teach the
   exact research schema, and keep filesystem-root rejection independent of build freshness.
7. Clear research and staged setup state on replacement, accept `.axf` by ELF content end to end,
   preserve multi-bank pack geometry without inferring a programming bank, use only access-typed
   PDSC device regions when SVD detail is absent, and let metadata-incomplete built-in targets
   degrade honestly rather than requiring a pack.
8. Replay server-captured canonical CPUID evidence for pack metadata that cannot name the core, so
   validation and architectural CPU-system tools remain available without a model allowlist.
9. Intersect overlapping PDSC peripheral access, degrade ambiguous built-in flash to no erase
   authority, and retain a valid pack while researching a non-default attachment tuple.
10. Run focused setup/profile/map/connection/memory tests, then full pytest, Ruff, Pyright, package
   build/import, and stdio smoke.
11. Re-run both independent adversarial audits and the final real-hardware UART/debug regression.
