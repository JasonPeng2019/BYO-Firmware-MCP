You are the hardware-in-the-loop operator. Use ONLY the MCP server named `byo-hil-setup-relock-20260723-174153`, which runs the current BYO-Firmware-MCP working tree. Do not use any other MCP server. You are explicitly authorized for setup, connection, validation, and deliberate disconnect on the attached nRF52840 DK or NUCLEO-L476RG, but NOT flash, erase, recovery, unlock, or security changes.

Work in this exact repository and do not edit production files, tests, plans, or config. All new evidence, packs, profiles, and reports must be under `testing_folder`. Inspect the live MCP tool surface and follow each live response/plan exactly; do not fabricate structured payloads or infer hidden tool contracts.

Goal: HIL reproduce and validate the latest setup-plan replacement/relocking fix in ONE server run. Fresh artifact/run roots are already configured. Choose whichever attached board the live inventory can truthfully identify and setup. Record exact part identity and board/probe information supplied by live routes.

Required state-machine test (not a generic happy-path setup):
1. Accept P1 setup plan and invoke board_setup such that it leaves a real incomplete continuation/allowance. Prefer an external-UART confirmation only if inventory truthfully reports it for the chosen board; otherwise use a genuine target/CMSIS-Pack research/setup continuation indicated by the server.
2. Without restarting/reconnecting the server, accept replacement P2 setup plan for the same board.
3. Invoke P2 board_setup, causing P1 to be retired.
4. Complete P2's real continuation exactly as returned, then invoke the paired board_fix_setup.
5. Demonstrate board_fix_setup is actually callable under P2 and reaches the server's truthful terminal or next setup state (do not call it twice absent live instruction).
6. Demonstrate that P2 matching closure relocks normally: query/use the live surface/next route such that the server explicitly shows the paired/primary setup action is no longer authorized, without attempting to bypass the plan system.
7. Deliberately disconnect through the live route.

If P1 cannot be left incomplete because the live setup completes synchronously, stop and report BLOCKED with exact live response: do not fake continuations. If the bug still happens, stop immediately after capturing exact tool evidence and report FAIL; do not edit source. If the chosen board requires a pack/download, keep it under testing_folder and follow server instructions. Avoid any destructive operation.

Write `testing_folder/HIL_RESULTS.md` with PASS/FAIL/BLOCKED, server run evidence, board/part identity, full P1/P2 sequence, exact evidence paths, tool outputs/summaries, what was not exercised, and disconnect outcome. Also write `testing_folder/agent/nested-agent-summary.md` with a concise outcome for the parent. Finish with a concise report.
