# Adjudication ledger

One row per finding from `reviews/iter-1-diff.md` and `reviews/iter-1-code.md`, adjudicated
by the implementer's directive. Appended to in later iterations rather than replaced.

| ID | Description | Adjudication | Reason | Resolution |
| --- | --- | --- | --- | --- |
| C1 | `_resolve_serial_port_for_session` unconditionally spawns `native_probes()` (a real `pyocd list --probes` subprocess) on every UART action, via `_hardware_inventory.snapshot()`. | VALID | Confirmed: the UART hot path never touched pyOCD before this work (base commit `6f3da0a` was pyserial-only); routing it through the full snapshot broke invariant 2. | Fixed (FIX 1): added `HardwareInventoryService.uart_snapshot()`, which never calls `native_probes()`, and rewired `_resolve_serial_port_for_session` onto it. |
| D1 | Same defect as C1, from the diff-review lens (docstring on `_resolve_serial_port_for_session` also claims zero subprocess cost, which was false as written). | VALID | Same root cause as C1; the diff adversary additionally flagged the now-false docstring. | Fixed (FIX 1): docstring corrected to describe `uart_snapshot()` and why it is safe. |
| C2 | `ProbeSelectionStore` has no cap, TTL, or eviction; a UID-less provider mints `connection_id` as `session:<uuid4>`, freshly random per connect, so connect/disconnect cycles accumulate entries forever. | VALID | Confirmed: only `refresh_discovery_hooks` clears the store, which a healthy server may never call; `DiscoveryRetryStore` in the same change is bounded, this was not. | Fixed (FIX 2): `ProbeSelectionStore` now backed by an `OrderedDict`, capped at `MAX_PROBE_SELECTIONS = 256`, oldest evicted on insert, re-recording an existing key refreshes its position. |
| D2 | UART tool timeout budget doesn't cover the new native probe-listing call (`_UART_ACTION_TOOLS` never received the `_PROBE_INVENTORY_TOOLS` addend for `native_probes()`). | VALID (resolved as a consequence) | Once C1/D1 is fixed and the UART hot path no longer calls `native_probes()` at all, there is no probe-listing cost left for the UART budget to under-cover. | Fixed as a consequence of FIX 1 -- no separate change needed; `uart_snapshot()` never invokes the probe-listing path this finding was about. |
| D3 | Guide's "append, don't replace" instruction allegedly violated for the probe-missing message in `setup_flow/preflight.py`. | INVALID (misattributed) | **Provenance corrected (was previously stated wrong; verdict and append-vs-replace reasoning are unchanged):** the locked-environment sentence was already in the working tree before this session touched the file, left by a parallel session's in-progress attempt at this same task, and required by that session's own untracked `test_preflight_probe_guidance.py` (later adopted, see M5). It is not, however, still an uncommitted edit -- `git diff HEAD -- src/pyocd_debug_mcp/setup_flow/preflight.py` is empty, and `git log -- src/pyocd_debug_mcp/setup_flow/preflight.py` shows it entered this repository's own history via commit `65386b2` ("Steps 5-8 wiring"), which needed to touch the same file to append the `hook_contract_call` remedy and, in doing so, committed the sentence that was already sitting in the working tree alongside it. Base commit `6f3da0a` does not contain it; this change correctly *appended* to the sentence as it existed in the working tree at the time. | Won't fix - invalid. Left untouched per instruction; now committed as part of this repo's own history via `65386b2`, not a lingering uncommitted edit. |
| C3 | Unhandled exceptions defeat the typed-failure guarantee in three places: (a) `execute_hook`/`_execute` don't guard `popen_owned`'s `Popen()` call; (b) `refresh_discovery_hooks` doesn't guard `run_hooks`; (c) the K1 TOCTOU in `_active_connection_rows` is reachable, unguarded, from two new `.snapshot()` call sites. | VALID | All three confirmed by reading the code: `_execute`'s `try:` starts *after* `popen_owned(...)` returns; `refresh_discovery_hooks`'s hook loop has no try/except; `_resolved_probe_uid_for_connection` and `_resolve_serial_port_for_session` are new exposure to the pre-existing K1 race (the pre-hooks UART path never scanned inventory at all). | Fixed (FIX 3): (a) `execute_hook` now catches `Exception` around `_execute(...)` and returns a typed `HookExecution` with a new `launch_failed` outcome; (b) `refresh_discovery_hooks` now catches `Exception` per hook-kind and returns a typed `discovery/hook-failed` payload naming the kind; (c) `_active_connection_rows` now uses `maybe_connection()` and skips `None` instead of `connection_for()`, and both new `.snapshot()`/`.uart_snapshot()` call sites (`_resolved_probe_uid_for_connection`, `_resolve_serial_port_for_session`) now catch `Exception` and re-raise typed (`TargetControlError` / `RuntimeError`) instead of propagating raw. |
| C4 | `MAX_HOOKS_PER_MANIFEST = 32` is per manifest; two manifests (project + operator) can each hit that cap independently, so 64 hooks x 60s sequential execution is ~64 minutes for one refresh, with nothing capping the aggregate. | VALID | Confirmed: sequential execution is by design (no concurrency), and the timeout budget is correctly sized for whatever count actually loads -- but nothing bounded how large that count could get across both sources. | Fixed (FIX 4): added `MAX_HOOKS_TOTAL = 48` in `discovery_hooks.py`, enforced in `load_hook_snapshot` after merging both sources (raises a typed `DiscoveryHookError` before anything executes), and surfaced as `limits.max_hooks_total` in `get_discovery_hook_contract`'s payload. |
| C5 | Content hash does not cover permission bits; a `chmod`-only change on an `executable` hook is invisible to the drift guard, so it reaches `popen_owned` and raises `PermissionError` unhandled. | VALID | Confirmed: `hook_source_digest` hashes file bytes only. This is the same root exposure as C3(a). | Fixed as a consequence of FIX 3(a) -- routing the `PermissionError` through the typed `launch_failed` outcome is the fix; no separate portable exec-bit check was added, per instruction. |
| C6 | `hooks_available` parameter on `no_native_probe_failure`/`no_native_uart_failure` is always `True` at every call site; dead conditionality. | VALID | Confirmed: `grep` showed no call site (production or test) ever passed `False`. | Fixed (FIX 6): parameter removed entirely from both functions; both now always offer the contract call unconditionally, matching the guide's actual intent. All call sites (2 production, 6 test) updated. |
| D4 | The J-Link UID-less-retry hook-immunity requirement (guide Trap 7) is covered only by a source-text substring scan (`test_the_retry_condition_consults_native_discovery_only`), not a behavioral test. | VALID | Confirmed: the existing test inspects `inspect.getsource(...)` for forbidden identifiers; it never constructs a scenario with a hook-discovered probe row present and checks the actual retry verdict. | Fixed (FIX 5): added `test_a_hook_discovered_matching_probe_row_does_not_change_the_retry_verdict` to `tests/test_unified_inventory.py::JLinkRetryIsolationTests`, which runs a real hook (via the fake-hook fixture) that reports exactly one matching `jlink` probe, and asserts `_single_matching_probe_visible_for_board_family` / `_should_retry_without_uid` return the identical verdict with and without that hook row present. The static substring guard is kept alongside it. |
| D5 | Guide's "stay green untouched" instruction for `test_server_assignment_connect.py` and `test_validation_honesty.py` was not honored (both were modified). | INVALID (deviation is required and disclosed, not a defect) | The guide's step-0 "untouched" constraint scopes to step 0; step 5 explicitly mandates the opaque-token contract change that requires these tests' expected values to change shape. `reviews/phase0-notes.md` discloses this under "Test contract changes required by step 5," and the diff reviewer independently confirmed no assertion was weakened -- only the literal token asserted changed, and the mismatch/conflation checks are intact. | Won't fix - invalid. Left as is. |
| D6 | `hooks_available` parameter always `True`; dead conditionality (diff-review restatement of C6). | VALID (duplicate of C6) | Same finding, same evidence, from the diff-review lens. | Fixed (FIX 6) -- see C6. |
| D7 | `test_a_refused_wrong_kind_ticket_runs_nothing_through_refresh_either` never calls `refresh(...)`; it duplicates the test above it and its name claims behavior it doesn't test. `refresh_discovery_hooks` is kind-agnostic by design (no `kind` argument). | VALID | Confirmed: the test body calls `self.contract("uart", ticket.retry_id)` a second time, not `self.refresh(...)`. The comment even admits "the ticket is valid for refresh." | Fixed (FIX 7): renamed to `test_a_second_wrong_kind_contract_call_also_refuses_without_reloading` with a docstring stating plainly what it actually asserts, and added a new test, `test_refresh_is_kind_agnostic_by_design_and_accepts_any_valid_ticket`, that actually calls `refresh(...)` with a ticket issued for one kind and asserts it succeeds -- documenting the real (intended) behavior instead of leaving it unasserted. |
| C9 | `ProbeSelectionStore.resolve()` returns on the `row is not None` path without touching `OrderedDict` recency, so a successful resolution against an actively-used entry is no better protected from eviction than one nobody has touched. | VALID | Confirmed: only `record()` and eviction touch ordering; a repeated, successful `resolve()` against an already-recorded connection_id never called `record()` again. This is what makes an eviction-driven misresolution realistic rather than theoretical. | Fixed (FIX 9): the `row is not None` success path in `resolve()` now calls `self.record(selection)` before returning, matching the "re-recording refreshes position" contract `record()` already documents. Test: `tests/test_probe_selection_records.py::BoundedStoreTests::test_a_repeatedly_resolved_entry_survives_eviction_pressure` interleaves `resolve()` calls with unrelated churn far exceeding the cap and proves both that the resolved entry survives and that an untouched entry from the same era is actually evicted (ruling out a vacuous test). |
| C10 / D9 | `MAX_HOOKS_TOTAL` (FIX 4) was enforced only after every declaration from both sources was path-resolved and SHA-256 hashed, so a maximal 32+32 manifest pair paid up to ~64MB of file I/O and 64 symlink-safe path-containment resolutions before rejection. | VALID | Confirmed: `resolve_declaration` (hashing + containment) ran inside the same loop that built `specs`, and the cap check sat after that loop and the sort. Not the sequential-execution-time cliff FIX 4 targeted (nothing executes), but a real, uncapped-until-the-end I/O cost. | Fixed (FIX 10): `load_hook_snapshot` now parses declarations from both sources first, counts them, and raises `DiscoveryHookError` on `total_declared > MAX_HOOKS_TOTAL` *before* calling `resolve_declaration` on anything. The original post-merge check is kept as a zero-cost second guard. Tests: `tests/test_discovery_hook_registry.py::AggregateHookCapTests::test_no_hashing_occurs_when_the_aggregate_cap_is_exceeded` (spies on `resolve_declaration`, asserts zero calls) and `::test_missing_entrypoints_never_surface_when_the_aggregate_cap_is_exceeded` (black-box companion: entrypoints that don't exist would raise a different, "not a file" error if resolution ever started; the cap error is what's actually raised). |
| C11 / D10 | The FIX 3b `except Exception` backstop in `refresh_discovery_hooks` returns a payload missing `retry_id`/`refresh_call`/`board_id`, which the normal partial-failure path always attaches when a ticket is present -- a thinner, less actionable response for the same class of failure. | VALID | Confirmed: the backstop built its JSON payload independently of the `ticket is not None` block at the bottom of the function, so it never reached that code. Low severity since FIX 3a now absorbs almost every exception this backstop exists for, but a real gap if it ever does fire. | Fixed (FIX 11): extracted `_failure_retry_fields(ticket)` as the one place that builds the `retry_id`/`refresh_call`/`retry_call`/`board_id` shape for a failed response, and both the backstop and the normal partial-failure branch now call it -- they cannot independently drift apart again. Test: `tests/test_discovery_retry_store.py::RefreshUnhandledExceptionGuardTests::test_the_backstop_carries_the_same_retry_affordance_as_the_normal_failure_path`. |
| M1 | `HookSourceChangedError` (`discovery_hooks.py`) is defined and never raised, caught, or referenced anywhere in `src` or `tests`. Dead code. | VALID | Confirmed by grep across the whole tree: only the class definition itself matched. The actual source-drift detection mechanism is the `"source_changed"` `HookOutcome` literal plus `_refusal(spec, "source_changed", ...)` in `execute_hook`, which does not use this class at all. | Fixed (FIX 12): deleted. Nothing else needed updating since nothing referenced it. |
| M2 | `list_connected_probes_detailed` (step 3's named deliverable) had zero direct tests; every existing test either constructed a `NativeProbeListing` by hand or exercised the CLI path only incidentally through a higher-level flow. | VALID | Confirmed via grep: no test file called `list_connected_probes_detailed` or `list_connected_probes_cli` directly to check its own diagnostics contract (attempt sequencing, exit-code classification, summarization). | Fixed (FIX 13): new `tests/test_probe_inventory.py::DetailedListingTests`, covering: no commands configured (empty sentinel), probes found on the first command, diagnostics belonging to the successful attempt (not the first) when an earlier command parses to zero rows, diagnostics belonging to the first/canonical attempt when every command fails, `exit_code == 124` setting `timed_out` (and `127` not setting it), `available` true only for exit 0, stdout/stderr truncation with the marker, the `list_connected_probes_cli` list-with-identical-contents wrapper contract, and `diagnostic_row()` shape (including the empty sentinel's). |
| C7 / D8 | `_setup_overview` mints `connection_id` from UID text alone (`probe_connection_id(uid)`), so two different providers reporting identical UID text collapse to one dedup key and one of two real, simultaneously-attached probes is silently dropped from `connections[]` -- not an ambiguity the friendly-selection flow can resolve, since ambiguity presents both choices and this removes one. `HardwareInventoryService` already keeps such rows distinct at the merge layer per the guide's own mandate; `_setup_overview` threw that away downstream. | VALID | Reproduced directly per the coordinator's repro and confirmed by reading `_matching_probe_index` (provider-aware) versus the old `probe_connection_id`/`_setup_connection_key` (provider-blind). | Fixed (FIX 8): `probe_connection_id` now takes `(provider, probe_uid)` and mints `probe:{provider}:{uid}` (percent-encoded, so a literal colon in either field can never be mistaken for the delimiter). `_setup_overview`'s row loop, `stable_connection_identity`, `_setup_connection_key`, `_same_setup_connection`, `_connection_matches_probe`, and `_selected_setup_connection_matches` all updated to be provider-aware; a legacy 2-part `probe:{uid}` token is still tolerated on read (equality-only, UID-text comparison) via `parse_probe_connection_id` returning `None` for it. Tests: `tests/test_setup_overview_no_probe.py::CrossProviderCollisionTests` (two providers with the same UID text both stay visible and independently selectable through `_setup_overview`, resolve to their own provider, and decimal-normalized collisions across providers stay distinct) plus the rewritten `ProbeConnectionIdTests`. |
| C8 | `derive_selection_from_token` (the `ProbeSelectionStore.resolve()` fallback for a connection_id that predates any recorded selection, or -- after FIX 2's bounding -- one that was evicted) matched on UID text alone with no provider filter, unlike `find_selected_row` (the *recorded*-selection path), which already filtered by provider. A cross-provider UID collision would resolve to whichever row happened to iterate first, silently. | VALID | Confirmed empirically and by reading the function: no `row.provider` check anywhere in the old implementation. | Fixed (FIX 8): a canonical (provider-qualified) token now filters snapshot rows by provider before matching UID, exactly like `find_selected_row`. A legacy 2-part token (or bare UID/session token) matches on UID text across all providers as before, but now counts DISTINCT providers among the matches and returns `None` (refusing, not guessing) when more than one provider matches -- the caller (`ProbeSelectionStore.resolve`) then raises the typed `SelectionDisappeared`/`SelectionNotRecorded` refusal. Tests: `CrossProviderCollisionTests::test_a_legacy_ambiguous_token_is_refused_not_guessed` and `::test_a_legacy_unambiguous_token_still_works`. |
| M3 | (Coordinator's own finding, flagged as the reason FIX 8 was risky.) Three raw `f"probe:{probe_uid}"` mint sites at `server.py` (`_stamp_validation_session`, the `commit_mismatch` closure inside `_record_validation_mismatch`, and `_record_validation_mismatch`'s own `provisional_connection_id`) bypassed `probe_connection_id` entirely -- two of the three were not even casefolded. Both `provisional_connection_id` sites feed `assignment_store.run_if_current`, which does exact string-key comparison (not the fuzzy `_same_setup_connection`), so leaving them on the legacy 2-part form while `_setup_overview` now mints the 3-part canonical form would make every validation stamp/mismatch-record call after FIX 8 report "assignment changed," unconditionally, not just in a rare edge case. | VALID | Confirmed by reading `RunAssignmentStore.run_if_current`/`.require` (`setup_flow/setup.py`): plain `dict` key equality on the literal string, never routed through `_same_setup_connection`. The coordinator's suggested "keep the 3 sites on the legacy 2-part form, make comparison helpers tolerant" therefore could not by itself preserve stamping in the common case, since `run_if_current` never consults those comparison helpers at all. | Fixed: added `_known_provider_for_board` (prefers the live connection's session metadata; falls back to the board's profile `probe_family` when the connection has already gone) and `_provisional_setup_connection_id` (routes through the one canonical `probe_connection_id` mint when a provider is known; falls back to the legacy provider-less form -- which safely fails `run_if_current`'s exact match rather than guessing -- only when neither source is available). All three sites now route through these. This is a deliberate refinement of the coordinator's literal suggestion, not a straight implementation of it: it makes the common case (live connection present, which both `_stamp_validation_session` call paths guarantee, and `_record_validation_mismatch` has in all but a genuine mid-flight disconnect race) produce the exact canonical key `_setup_overview` stored, rather than relying on comparison-helper tolerance that `run_if_current` never actually consults. Reported to the coordinator per "if it starts cascading into places I did not list, stop and tell me what you found." Tests: `tests/test_server_assignment_connect.py::test_a_validation_mismatch_clears_the_assignment_and_records_the_gate_mismatch` (live-connection path, using the real `RunAssignmentStore`, not a stub) and `::test_a_mismatch_after_the_connection_already_vanished_still_matches_via_the_profile` (profile-fallback path). |
| D8 | Same defect as C7, from the diff-review lens (`_setup_overview` silently drops a real, distinct probe when two providers report identical UID text). | VALID (duplicate of C7) | Same finding, same evidence, from the diff-review lens. | Fixed (FIX 8) -- see C7. |
| D9 | Same defect as C10, from the diff-review lens (`MAX_HOOKS_TOTAL` enforced after, not before, the expensive per-declaration resolve/hash work). | VALID (duplicate of C10) | Same finding, same evidence, from the diff-review lens. | Fixed (FIX 10) -- see C10. |
| D10 | Same defect as C11, from the diff-review lens (the FIX 3b exception backstop returns a thinner payload than the normal partial-failure path). | VALID (duplicate of C11) | Same finding, same evidence, from the diff-review lens. | Fixed (FIX 11) -- see C11. |
| C12 / D11 | `parse_probe_connection_id` discriminated canonical-vs-legacy by colon *count* ("does the text after `probe:` contain a second colon"), not by structure. A legacy token whose UID contains a colon -- realistic for hook-reported values; the guide's own cross-platform hook guidance points authors at USB topology values like Linux's `3-1.4:1.0` -- misparsed as canonical, in the worst case silently resolving to a different, real, unrelated probe. The prior docstring's "vanishingly unlikely" rationale covered native pyOCD UID formats only, not hook-reported ones, which is the path this feature exists to support. | VALID | Confirmed and reproduced directly by both reviewers, independently: a legacy token `probe:3-1.4:1.0` misparsed into provider `"3-1.4"`, uid `"1.0"` (present probe reported as gone); a legacy token `probe:jlink:001` for a cmsisdap probe misparsed into provider `"jlink"`, uid `"001"`, resolving to an unrelated real jlink probe that happened to report uid `"001"` (wrong-hardware selection -- the severe case). | Fixed (own commit, before anything else this round): canonical `connection_id` now uses a prefix (`probeid:`) structurally distinct from the legacy `probe:` prefix -- no legacy token, regardless of UID content, can ever start with it, so `parse_probe_connection_id` is now a pure prefix check with no colon-counting or content inspection anywhere. Swept every consumer: `derive_selection_from_token`, `_connection_matches_probe`, `_same_setup_connection`, `_setup_connection_key`, the M3 provisional-connection-id fallback. Confirmed the prefix is never persisted to disk (gate state lives on `ServerRun`, explicitly documented as memory-only and never surviving a restart) and is not documented in the client contract, so this was not a migration question. Regression tests for both reproductions: `tests/test_probe_selection_records.py::TokenDerivationTests::test_a_legacy_uid_containing_a_colon_still_resolves_to_its_own_probe` and `::test_a_legacy_uid_containing_a_colon_never_resolves_to_a_different_probe`, plus direct structural-discriminator tests in `tests/test_setup_overview_no_probe.py::StructuralLegacyDiscriminationTests`. **Strengthened by iteration 4 (directed target 2):** the "never persisted, not a migration question" framing above undersells why the legacy `probe:` path must be kept -- its live reachability does not depend on anything surviving a restart at all, because it needs none of that state. `derive_selection_from_token` (`hardware_inventory.py:765-823`), invoked by `ProbeSelectionStore.resolve()` whenever the presented token was never recorded (first use, evicted past `MAX_PROBE_SELECTIONS`, or the store was cleared), re-derives a selection purely by parsing the token text against the *current* snapshot -- it consults no stored assignment. And the token it parses is not always server-minted: `board_validate`'s optional `probe_id` argument (`tools/setup.py:535`) is plain free-text client input that reaches this exact path via `_select_probe`/`_connection_matches_probe` (`server.py:3669-3695`). So on every server run, indefinitely, an agent that types, remembers, or copies a `probe:{uid}`-shaped string into `probe_id` exercises the legacy path as ordinary, expected client behavior -- not an exotic pre-upgrade replay scenario. The path is live, client-reachable, ordinary-path code and must not be deleted as unreachable or vestigial; the fix is correct as implemented (refuses on cross-provider ambiguity per C8, cannot collide with a canonical token per this fix's structurally distinct prefix). |
| C13 / D12 | `_known_provider_for_board`'s profile-fallback safety claim (M3) was asserted in its docstring but not tested for the case that would matter: whether the profile's declared provider can ever diverge from the vanished live connection's actual provider, producing a canonical-looking key that matches a *different* real probe (worse than the documented "safely fails" outcome). The existing test constructed both the profile and the stored assignment from the same fixture, so it could not detect a divergence even if one existed. | VALID -- correctly reported by the reviewer as an untested assumption, not a confirmed defect | Per instruction, wrote a test with genuinely independent, divergent profile and stored-assignment providers and observed the actual behavior rather than assuming it, with instructions to stop and report if it turned up a real wrong-attribution case. | Confirmed safe; no code change required. `_provisional_setup_connection_id` mints from the diverged (profile-fallback) provider, which is then simply not the stored assignment key for that board_id -- `run_if_current`'s two-way exact-match dictionary lookup requires equality against the value stored for *this exact board_id*, so a wrong provider can only ever produce "no match" (fails closed: returns `False`, nothing recorded, assignment left untouched), never a match against a stored key naming a different real probe. Test: `tests/test_server_assignment_connect.py::test_a_stale_divergent_profile_provider_fails_closed_not_wrong`. |
| C14 / D13 | `tests/fake_discovery_hook.py::_counter_value`'s lock-acquisition retry loop caught only `FileExistsError`; on Windows, the same "someone else holds the lock" race can surface as `PermissionError` (WinError 5) instead, crashing the fake hook and causing rare, reproducible flakiness in `tests/test_inventory_snapshot_concurrency.py::test_concurrent_snapshots_are_internally_consistent`. | VALID (test-scaffolding defect, not a race in `HardwareInventoryService`/`discovery_hooks.py`) | Confirmed pre-existing (single commit `3bcdb7a`, untouched by any iteration's fixes) and root-caused by both reviewers to the same Windows-specific `os.open(O_CREAT\|O_EXCL)` behavior; `HardwareInventoryService`'s own atomicity guarantees showed no inconsistency in any reproduction. | Fixed: the retry loop now catches `(FileExistsError, PermissionError)`, with a comment naming the Windows behavior and cross-referencing how `kernel/processes.py` already treats this class of OS-error variance as expected rather than exceptional. Verified with repeated runs of the concurrency test file after the fix (no failures). The coordinator overruled the reviewer's suggestion to mark the test green with an explanatory comment instead of fixing the scaffolding. |
| D14 | The "617 passed / 7 skipped, ruff clean" baseline reported through iteration 3 was not reproducible from a clean checkout of the commits under review; it silently depended on an uncommitted, out-of-scope working-tree change (`src/pyocd_debug_mcp/probe_families.py`) and two untracked test files, all three left by a parallel session's independent attempt at this same task. | VALID | Confirmed: stashing only `probe_families.py` while leaving the two untracked test files in place (reproducing what a clean checkout of `ced6231` plus those two leftover files would actually contain) reproduced exactly one failure, `tests/test_probe_cli_command.py::test_default_inventory_uses_the_server_interpreter`; the other untracked test and the rest of the 617 were unaffected. Our own committed work through `ced6231` was 614 tests, not 617 -- prior "suite is green" reports in this ledger and in iterations 1-3 were accurate about *this working directory* but overstated what was actually committed. | Resolved by adopting the modification and its two tests as this repo's own rather than discarding them -- see M4/M5. The tree now legitimately reproduces 617 passed / 7 skipped from a clean checkout as of commit `f4d5ecf`. |
| C15 | `tests/test_swd_process_isolation.py` has hardcoded 10s `subprocess.run` timeouts (three sites) and a hardcoded 0.5s/0.7s shared-deadline budget in `test_startup_and_first_call_share_one_absolute_deadline`, both too tight to reliably survive real machine load -- reproduced at roughly 1-in-5 suite runs. One of the two failure modes races real production code (`src/pyocd_debug_mcp/adapters/swd_process.py`'s startup-deadline path, `:241,310-312`), which the reviewer flagged as warranting a closer look before concluding it was test-only rather than a genuine race. | VALID | Diagnosed by reading `swd_process.py`'s deadline machinery end to end (`_operation_deadline`, `_remaining`, `_read`, `_write`, `_WorkerClient.__init__`, `_WorkerClient.call`) before touching anything, per instruction: all use `time.monotonic()` consistently, fail closed the instant a deadline has already passed, and `call()` reuses a caller-supplied `deadline` verbatim rather than computing a fresh one -- the "one shared absolute deadline" contract is implemented correctly. No production bug found; this is case (a) (test budget too tight), not case (b) (genuine production race). Root cause: the three `subprocess.run` timeouts are bare literals unrelated to any production constant, and the shared-deadline test's fixed 0.5s budget raced `fake_provider_worker.py`'s real process spawn plus its own deliberate 100ms `ready_then_hang` delay, with construction sitting outside the test's `assertRaises` -- a load hiccup during spawn surfaced as a test ERROR, not a clean assertion failure. | Fixed: the three `subprocess.run` timeouts now use `DEFAULT_EXTERNAL_COMMAND_TIMEOUT_SECONDS` (`pyocd_debug_mcp.timeouts`, 30s) instead of a hardcoded `10`, deriving from the same constant production uses for external-command timeouts rather than picking a bigger arbitrary number. `test_startup_and_first_call_share_one_absolute_deadline` widened to 2.0s/2.5s (not derived from the 30s constant, which would make this otherwise-fast contract test slow) and restructured so worker construction is covered by the same `assertRaises` as the call, since the actual invariant under test is that the shared deadline is honored, not specifically where. Verified with 8 isolated back-to-back runs of the file (30/30 clean each run), plus a full-suite run (617/7 skipped) -- the same repeated-run method used to reconfirm C14. Commit `2e07463`. |
| M4 | Adopt `src/pyocd_debug_mcp/probe_families.py`'s uncommitted modification -- routes the default `pyocd` CLI fallback through `(sys.executable, "-m", "pyocd", ...)` instead of resolving `pyocd` off PATH via `shutil.which` -- left in the working tree by the same parallel session as D14, rather than discard it as unrelated leftover work. | ADOPTED (coordinator ruling) | The implementation guide states at lines 494-496, as an established fact, that `configured_probe_cli_commands` already routes pyOCD through `sys.executable` "so hooks run in the same locked env," and `discovery_hooks.py:161-166` -- this session's own, already-reviewed code -- implements the `server-python` hook runner on exactly that premise, with a comment naming the same rationale. At `ced6231` the premise was false: `configured_probe_cli_commands` resolved the default executable off PATH, a different interpreter/environment than the one hooks execute in whenever the two differ. This is a real correctness gap, not bookkeeping -- the §0 gating rule decides whether hooks run at all based on whether native probe enumeration came back empty, so gating could be decided by a different Python environment than the one hooks then execute in. `git log --oneline -- src/pyocd_debug_mcp/probe_families.py` confirms no commit in this feature's implementation ever touched the file; the fix the guide's own text assumed already existed was never actually made in this repo's history. Reviewed on its own merits before adopting, not merely for passing tests: preserves the `PYOCD_CLI` operator-override path unchanged; the new `sys.executable` branch adds no validation (no empty-string/null-byte check) where the override and `shutil.which` branches both validate, but this matches the already-reviewed `discovery_hooks.py:161-166` precedent exactly (same construct, same lack of validation, same rationale), so it is not a new weakness introduced by this diff; ruff and pyright both clean. | Adopted verbatim, commit `f4d5ecf` ("Phase 1: adopt locked-interpreter probe CLI fix and its tests (M4/M5, resolves D14)"). Disclosed explicitly rather than folded in silently: this is another session's code, now carrying this repository's name, and this ledger entry records that provenance instead of presenting the change as originating from this feature's own implementation work. |
| M5 | Adopt `tests/test_preflight_probe_guidance.py` and `tests/test_probe_cli_command.py`, both left untracked by the same parallel session as D14/M4, rather than discard them. | ADOPTED (coordinator ruling) | Both confirmed to cover genuine gaps, not duplicates of existing coverage, by grepping every other reference to the functions they test: no other test exercises `PreflightEngine.evaluate`'s `setup/no-probe` branch or asserts on its `agent_prompt` content -- the superficially similar `tests/test_setup_overview_no_probe.py` tests a different function (`server._setup_overview`) with different status codes (`setup_no_probe`) and different message text, not `PreflightEngine`'s `setup/no-probe` code path at all; and no other test asserts `configured_probe_cli_commands`'s actual argv/executable-resolution behavior -- every other reference (`test_server_assignment_connect.py`, `test_probe_inventory.py`, `test_discovery_hook_safety.py`) either mocks the function outright or calls it only for `len(...)` in timeout-budget arithmetic. `test_preflight_probe_guidance.py`'s dependency -- the "locked environment" sentence in `preflight.py` -- is already committed, reviewed code (see corrected D3 above), not an uncommitted stray; adopting the test required no source change at all. `test_probe_cli_command.py::test_default_inventory_uses_the_server_interpreter` depends on M4's source fix (fails against `ced6231` unmodified); `::test_explicit_operator_override_remains_supported` is independent of M4 and passes against HEAD either way. | Adopted verbatim alongside M4, commit `f4d5ecf`. |
| D15 | Guide step 4 (lines 683-688) requires `SERIAL_FALLBACKS` and its two parsers to "move behind the unified layer as a third provenance source, `("vendor:nrfjprog",)` etc." The data type (`VendorUartRow`), the injection point (`HardwareInventoryService.vendor_uarts`), the merge path (`_vendor_uart_rows`/`_collect_uart_rows`, sharing the §0 UART gate) and thorough unit tests (`test_unified_inventory.py::VendorProvenanceTests`) were all built -- but nothing ever produced a `VendorUartRow` in production. `vendor_uarts` was the only one of five constructor parameters left at its `lambda: ()` default at the sole construction site, `server.py:3010-3015`, so the merge was permanently a no-op. | VALID | Confirmed by the coordinator directly, not taken on report: `grep -rn "VendorUartRow" src/` matched only the dataclass definition and the merge method's local use -- the type was constructed nowhere in `src/` at all, only by hand in the test file. Read guide step 4 verbatim to confirm the requirement is explicit rather than inferred. This is a Phase 0 completeness defect ("implement exactly what the guide describes -- nothing more, nothing less"), and a defect class the loop was blind to for four iterations: a feature scaffolded, unit-tested, and green, yet unreachable in production. | Fixed: added `_nrfjprog_vendor_rows`/`_stlink_vendor_rows` (thin adapters over the two parsers, both left byte-for-byte unmodified per the guide), `_VENDOR_FALLBACK_ADAPTERS` dispatching on `spec.parser` (already restricted by `_load_serial_fallbacks` to `{"nrfjprog_com", "stm32_programmer_list"}`, so no if/elif chain and no new field), and `vendor_uart_rows(run_cmd)`; wired at `server.py` as `vendor_uarts=lambda: vendor_uart_rows(_run_cmd)`. Placed in `hardware_inventory.py`, not `serial_resolver.py`, because the latter would invert an existing import direction and create a cycle. `usb_serial`/`vid`/`pid` deliberately left `None` -- neither parser yields them, and fabricating one would wrongly mark a row `stable` in `_uart_scope`. **The coordinator overruled the reviewer's severity reasoning here.** The reviewer argued the finding was near-harmless because even fully wired the feature would be structurally dead, since `_resolve_nordic_serial` can only match ports pyserial already returned (`_find_port_by_name`, `serial_resolver.py:507`). That traces the wrong call path: the guide says to move the *parsers* behind the layer, and the parsers turn vendor-CLI text into port names with no pyserial involvement -- which is exactly what makes the feature meaningful in the only case it is ever consulted (native enumeration returned nothing). Wiring it through `resolve_serial_port` as the reviewer's framing implied would have produced an inert fix that satisfied the letter of the finding. Note `SERIAL_FALLBACKS` is `()` unless `PYOCD_SERIAL_FALLBACK_REGISTRY` is set, so this is opt-in by design, per the guide's "keep it working for one release." |
| C16 | Same defect as D15, from the code-adversary/cold-read lens: a fresh reader cannot discover that `vendor_uarts` is unwired without independently checking `server.py`'s single construction site, ~2800 lines away from the well-tested class the parameter belongs to. | VALID (duplicate of D15) | Same finding, same evidence, from the cold-read lens. Recorded rather than folded into D15 because the framing is itself the point: the wiring gap was invisible from the tested unit, which is why four passes missed it. | Fixed (see D15) -- no separate change. |
| D16 | The iteration-4 fix for C15 moved `_WorkerClient` construction *inside* the same `assertRaises(TargetConnectionError)` as the `.call()` it exists to guard, in `tests/test_swd_process_isolation.py::test_startup_and_first_call_share_one_absolute_deadline`. Because `_WorkerClient.__init__` performs its own deadline-bound handshake read and routes any failure through `_invalidate` (typed `NoReturn`, always raises `TargetConnectionError`), construction alone can satisfy the assertion -- so the test can pass green while `.call()`'s deadline-sharing logic, the entire point of the test, never runs. | VALID | Proven empirically by the reviewer against the real `_WorkerClient`, not inferred: with an already-expired deadline, `TargetConnectionError` was raised from `__init__`, a spy recorded `.call()` invoked **0 times**, elapsed was 0.031s, and both assertions passed clean. The reviewer separately verified the *other* half of the C15 fix is sound -- monkeypatching `.call()` to ignore the caller's deadline and compute a fresh `time.monotonic() + 10` still fails `assertLess(elapsed, 2.5)` at 10.27s -- so the 2.0s/2.5s widening does not mask a broken deadline share. The widening was the correct part of the C15 fix; moving construction inside `assertRaises` was gratuitous and is what destroyed the coverage. Note this is the second time in this task that a fix introduced a defect (cf. C12, introduced by FIX 8 in iteration 2). | Fixed: construction restored to outside `assertRaises`, keeping the widened 2.0s/2.5s budget (~7x headroom over ~0.27s typical construction, so the original ~1-in-5 C15 flake stays fixed); only `.call()` remains inside. Added `assertEqual(client._request_id, 1)` to prove positively that `.call()` was entered rather than relying on structure alone, plus `try/finally` cleanup. **Verified the test can now fail**: with `_remaining(call_deadline)` deleted from `_WorkerClient.call`, the test failed with `AssertionError: 10.375 not less than 2.5`; the break was then fully reverted. The C15 row above should be read together with this one -- "C15: fixed" does not by itself mean the shared-deadline test was airtight. |
| M6 | (Coordinator's own finding, from re-reading the unreviewed D15 fix after Phase 3 was already written.) D15 made `HardwareInventoryService.snapshot()` invoke one vendor CLI subprocess per `SERIAL_FALLBACKS` spec, each at `_run_cmd`'s 30s default, but no operation timeout budget accounted for them. `_PROBE_INVENTORY_TOOLS`'s budget (`kernel/operations.py`) summed probe CLI commands and hooks only -- its own comment enumerated exactly those two sources. With vendor specs configured and a hung vendor tool, a snapshot could consume time the budget did not know about and be cancelled mid-discovery by the timeout that exists to prevent that. | VALID | Confirmed by reading the budget arithmetic against `vendor_uart_rows`. Gated (opt-in via `PYOCD_SERIAL_FALLBACK_REGISTRY`, and only when native UART enumeration is empty), so MEDIUM rather than HIGH. Falsified this ledger's own prior claim, and `TASK_STATUS.md`'s, that there were no known unfixed defects at HEAD. | Fixed, commit `459524b`. **The coordinator's analysis was incomplete and the implementer corrected it**: the brief named only `_PROBE_INVENTORY_TOOLS`, but `_UART_ACTION_TOOLS` reaches the identical `_collect_uart_rows()` path via `uart_snapshot()`, and `read_serial`/`write_serial`/`serial_exchange` each take an argument-driven early-return branch that discards `resolved_timeout` entirely -- so the generic fix had *no effect* for those tools until applied to all three branches, demonstrated by a test that first failed with a 0.0s delta. `_DISCOVERY_HOOK_TOOLS` traced and correctly excluded (`refresh_discovery_hooks` calls `services.load_snapshot()`/`run_hooks()` directly, never reaching `HardwareInventoryService`), with a regression test pinning its budget flat. `SERIAL_FALLBACKS` imported directly into `kernel/operations.py`: `serial_resolver` has zero internal package imports, so it is a leaf module that cannot close a cycle back through `kernel/__init__`. |
| D17 | The M6 fix covered five budget sites and missed a sixth. `include_finalizer` (`kernel/operations.py:568-580`) reserves `_hook_budget("uart")` for a UART `on_exit` finalizer's second, independent port resolution -- its own comment states the reason, that `_finalizer_uart_write` calls `_resolve_serial_port_for_session` -- but never added `_vendor_uart_budget()` there. That is the same path reaching `uart_snapshot()` -> `vendor_uart_rows`, so the finalizer can invoke the vendor CLIs a second time with nothing reserved for it. | VALID | Proven by the reviewer, not inferred: with one configured vendor spec, `read_serial` + finalizer grows by only 31.5s where two independent resolutions require 63.0s. Confirmed by the coordinator reading the site directly. **This is the fourth consecutive instance of a fix introducing the next defect in this task** (C7->C12, C15->D16, D15->M6, M6->D17). | Fixed: `_vendor_uart_budget()` added alongside the existing `_hook_budget("uart")`, comment extended to name both sources. Completeness verified by construction rather than assumption -- `vendor_uart_rows` is reachable only via `_collect_uart_rows`, called only by `.snapshot()`/`.uart_snapshot()`; all six call sites of those in `server.py` enumerated and each confirmed behind an already-budgeted tool. |
| D18 | `VendorUartRowsTests`'s two "nonzero exit code" tests (124 timeout, 127 not-found) were vacuous. Both fixtures paired the nonzero exit code with *empty* stdout, so with the `if exit_code != 0: continue` guard removed, execution fell through to the parser, the parser received empty text, and returned no rows anyway -- the `assertEqual(rows, [])` assertion held for the wrong reason. | VALID | Proven by the reviewer deleting the exit-code guard from `vendor_uart_rows` outright: **all 8 tests still passed.** No other test in the suite exercises that guard. These were the specific failure-mode tests the coordinator commissioned to justify that a hung or missing vendor CLI cannot inject rows; as written they justified nothing. | Fixed: both fixtures now supply realistic parseable `nrfjprog --com` output alongside the nonzero exit code -- which is the case that actually occurs in production, since `_run_cmd` returns exit 124 together with whatever partial stdout the child produced before being killed (`subprocess_timeout_stream_text(exc.stdout)`). Re-proved by deleting the guard again: both tests now FAIL, reporting the concrete `VendorUartRow` objects wrongly produced. Guard restored. |
| D19 | Two of the four new "budget unaffected by vendor specs" `BudgetTests` patched `SERIAL_FALLBACKS` to `()` -- the value it already defaults to in the test environment -- making the before/after comparison mathematically incapable of detecting any regression. | VALID (LOW) | Proven by deliberate break. Severity LOW rather than MEDIUM because that break *was* caught by unrelated pre-existing tests in the same file, so the suite is not blind to the regression; these two tests simply are not what guards it, despite their names asserting they are. | Fixed: both rewritten as absolute-value assertions against independently derived expected constants (matching the existing `derived` closed-form precedent in `tests/test_server_assignment_connect.py`) rather than self-referential before/after deltas. Proved by breaking `_vendor_uart_budget()` to `(len(SERIAL_FALLBACKS) + 1) * (...)`: both failed (`157.0 != 125.5`; `61.5 != 30.0` across three subtests), then reverted. |
| D20 / C17 | The D17 fix (vendor budget at the `include_finalizer` site) is numerically correct but has **no regression test**. Every other budget site touched by M6/D17 is covered in `BudgetTests`; the finalizer site is the only one that is not, and there is no vendor-CLI equivalent of `test_read_serial_with_a_uart_finalizer_reserves_both_hook_budgets`. Current behaviour verified correct by hand; nothing in the suite would catch a future regression. | **MUST-FIX** | Ruled MUST-FIX rather than nice-to-have under the new materiality policy for a specific reason: a fix has introduced the next defect four consecutive times in this task (C7->C12, C15->D16, D15->M6, M6->D17), and every one of those sat in code lacking a test for exactly the property that broke. D17 is in that same code. One test is proportionate insurance -- it neither gold-plates nor makes the product harder to work with, which is the standard the policy sets. | Fixed: `test_read_serial_with_a_uart_finalizer_reserves_vendor_budget_twice` added to `tests/test_hook_gating_and_budget.py`, holding `read_serial` arguments fixed (with and without a UART `on_exit` finalizer) and toggling `SERIAL_FALLBACKS` empty -> one spec, asserting the delta is `2 x ONE_VENDOR_SPEC` with the finalizer and `1 x` without. Proved load-bearing by removing `_vendor_uart_budget()` from `include_finalizer` **only**, leaving the other five sites intact so the test could not pass off one of them: failed `31.5 != 63.0`. Break reverted and confirmed by the coordinator to match HEAD exactly. Suite 665. |
| D21 | The guide's step 4 instruction to "document precedence" for the vendor-as-third-provenance-source mechanism was never satisfied. `docs/`, `SERVER_GUIDE.md` and `README.md` contained zero mentions of `vendor:` provenance or its precedence against native and hook rows. | **MUST-FIX** (LOW severity) | Ruled MUST-FIX for consistency rather than severity: D15 was ruled valid precisely because it was a guide requirement not carried through end to end, and this is the remaining clause of that same instruction. Ruling it extraneous would apply a different standard to the two halves of one sentence in the guide. The cost is a documentation paragraph, not a code change. | Fixed in `docs/architecture.md`: documents the three provenance sources (`native`, `hook:<id>`, `vendor:<provider_id>`), the gating and precedence (native wins; hook and vendor rows are consulted only when native UART enumeration returns empty; neither can delete or override a native row), that vendor rows are opt-in via `PYOCD_SERIAL_FALLBACK_REGISTRY` and inert by default, retained for one release and slated for deprecation, and that vendor rows carry no `usb_serial`/`vid`/`pid` and are therefore always `session`-scoped, never `stable`. The implementer was directed to verify each claim against `_collect_uart_rows`/`_vendor_uart_rows`/`_uart_scope` before writing it rather than transcribing the coordinator's summary, on the grounds that documentation misdescribing the code is worse than none. **The first draft did exactly that and the coordinator corrected it before commit.** It stated vendor rows are "consulted only when both native and hook discovery come back empty" -- a stricter gate than the code implements. `_collect_uart_rows` (`hardware_inventory.py:413-419`) gates hooks and vendor rows *independently*, both on native rows being empty, and merges vendor rows **before** hooks run; the function's own docstring says so explicitly. The implementer reported "all claims verified against `hardware_inventory.py` source code" while shipping the opposite of what that file says. Corrected text now also documents the both-sources-can-appear consequence, which is the same behaviour recorded as D22 -- so the extraneous finding is disclosed to readers even though it was not fixed. |
| D22 | When a vendor helper **and** a UART hook are both configured, and native enumeration is fully empty, a vendor row and a hook row describing the same physical port do not merge -- their provenance strings differ -- so the agent sees two candidates for one device instead of one. | **EXTRANEOUS** | First finding ruled extraneous under `reviews/REVIEW_POLICY.md`. Reaching it requires a vendor helper configured *and* a UART hook configured *and* native enumeration completely empty *and* both sources naming the same physical port. The outcome is a duplicate selection candidate, which the existing ambiguity/friendly-selection flow already handles safely -- **no wrong hardware is selected and no data is corrupted.** Fixing it would mean adding cross-provenance merge logic to prevent a cosmetic duplicate in a configuration almost nobody runs, which is the "nitty-gritty of things that already work in 99.9% of cases" the policy exists to filter out. | **Not fixed, by decision.** Recorded here so it is neither silently dropped nor re-raised in a later iteration. Should the merge behaviour ever become material -- for instance if a future change let vendor rows reach a hot path, or if duplicate candidates began causing wrong selections rather than merely extra choices -- this entry is the starting point and the verdict should be revisited against the code as it stands then. Note the reviewer reported this despite expecting exactly this verdict, rather than pre-filtering it; that is the division of labour `REVIEW_POLICY.md` requires, and it working as intended is worth recording alongside the finding itself. |
| D23 / C18 | All three `on_exit` finalizer fixtures in `tests/test_hook_gating_and_budget.py` used `{"action": "uart_write", "data": "q", ...}`, but `UARTWriteFinalizer` (`kernel/finalizers.py:12-17`) declares `extra="forbid"`, `strict=True`, and a required `text: str`. `data` therefore fails twice over -- required field missing, forbidden extra present -- so every fixture raised `FinalizerValidationError` and fell through the **defensive fallback** branch. The well-formed branch had never been exercised by any test. | **MUST-FIX** | The reviewer filed this LOW, having proven the budget is 63.0 down either branch, so D20's guarantee was not undermined. The coordinator escalated it. Both branches set `finalizer_reaches_uart = True` independently, and the tests only ever traversed the invalid one -- meaning **the valid-finalizer path, which is the ordinary case for real users, had no budget coverage at all.** Breaking `finalizer_reaches_uart = True` in the well-formed branch would leave every test green while a user sending a perfectly valid `uart_write` finalizer received an under-reserved budget and a mid-operation cancellation. A test that stays green while a realistic user breaks is material regardless of how narrow it appears, per `REVIEW_POLICY.md`'s standing carve-out, and the fix was one word in three places. | Fixed: `"data"` -> `"text"` at all three sites. **No expected number changed**, confirming the two branches are numerically equivalent and that no assertion needed adjusting -- the implementer was instructed to stop and report rather than tune any assertion to match, since a changed number would have meant the branches diverge, a larger finding. Well-formed parsing confirmed directly rather than inferred, via a new test calling `parse_finalizer("read_serial", ...)` and asserting it returns a `UARTWriteFinalizer` with `text="q"`. Three independent break-and-revert proofs: removing `_vendor_uart_budget()` from `include_finalizer` fails the D20 test (31.5 != 63.0); removing `_hook_budget("uart")` fails its sibling (61.5 != 123.0); and -- the proof that validates this finding -- flipping `finalizer_reaches_uart = True` to `False` **in the well-formed branch only**, leaving the fallback untouched, now fails both tests, where before the fixture fix it would have passed silently. Noted in passing: the line-435 test still exercises the fallback for an unrelated and legitimate reason (`reset_and_run` is not in `ELIGIBLE_FINALIZER_TOOLS`, so `parse_finalizer` raises on the tool-name check before field validation), documented in its docstring. Suite 666. |
| D24 | `write_serial`, the other `ELIGIBLE_FINALIZER_TOOLS` member, has no dedicated finalizer-budget-doubling test; only `read_serial` does. | **EXTRANEOUS** | `include_finalizer` is a single shared closure, not per-tool duplicated code, so a `write_serial` test would re-execute identical lines already covered through `read_serial`. Adding it buys no new coverage and is precisely the duplicate-for-completeness work `REVIEW_POLICY.md` exists to filter. The reviewer itself flagged it as likely extraneous by structural reasoning before adjudication. | **Not fixed, by decision.** Should `include_finalizer` ever be specialised per tool -- so that `write_serial` and `read_serial` no longer share one code path -- this verdict must be revisited, because the reasoning above depends entirely on that closure staying shared. |
| D25 / C19 | `discovery/unsupported-provider` never fired. Nothing anywhere validated a hook-reported provider against `PROBE_CLASSES`, so a connect attempt on a probe whose provider pyOCD has no plug-in for fell through to a pre-existing `ProbeNotFoundError` -- telling the agent "no matching debug probe found," which reads as a cabling problem when the real answer is "install a pyOCD probe plug-in." An existing test, `test_an_unsupported_provider_is_diagnostic_only`, carried a name asserting a treatment its own assertions never checked and production never implemented. | **MUST-FIX** (HIGH) | Discovery hooks exist precisely so agents can surface hardware pyOCD does not natively support, so this misguidance lands hardest in the exact scenario the feature was built for -- not a narrow edge case but the core use case. Fourth test in this task found carrying a name it did not honour. Root cause shared with D26/D27/D28 and identical in shape to D15: a guide-mandated typed failure code implemented and unit-tested in isolation, never wired to the production call site that should trigger it. | Fixed. **Validation site chosen at selection time (`ProbeSelectionStore.resolve()`), not hook-row ingestion** -- ingestion-time rejection would either hide the row from `setup_overview` or force a reshape of `InventorySnapshot` to carry per-row diagnostics, and would contradict the diagnostic-only premise that a hook-found row stays visible (hiding it discards the one signal the hook exists to produce). `resolve()` is the single choke point both real connect paths already share (`_assigned_probe_uid_for_connect`, `_resolved_probe_uid_for_connection`) and already use for the structurally identical `SelectionDisappeared` refusal, so every path to `open_session` is gated before pyOCD's own misleading error can fire. Added `UnsupportedProvider` carrying `.provider`/`.registered_providers`, checked at both return points in `resolve()`; providers derive from the existing `registered_provider_ids()` source of truth, **no second list**. `except UnsupportedProvider` added beside the existing `SelectionDisappeared` handler at both server sites, using `unsupported_provider_failure()` unchanged. Test extended to call the real `resolve()` and assert it raises, then assert concrete payload contents (`code`, absence of `hook_contract_call`, provider name in `agent_prompt`, plug-in remedy) -- proving reachability rather than repeating the isolation mistake. No existing assertion changed. Suite 666. **Known residual gap:** the two `server.py` handlers themselves have no regression test -- deleting them fails nothing -- carried forward into the D28 dispatch. |
| D28 | `_no_native_probe_overview` selected `hook_failures[0]` with no kind filter and then hardcoded `"probe"`, so a failing **UART-only** hook -- with no probe hook configured at all -- was reported to the agent as a probe discovery failure, pointing it at the wrong `get_discovery_hook_contract` kind. | **MUST-FIX** (MEDIUM-HIGH) | Rated below D25 because the raw `hook_diagnostics` array still carried the truth, so the information was present even when the summary was wrong -- but the summary is what an agent reads first and acts on. Same cluster and same root cause as D25/D26/D27. | Fixed: filters `hook_failures` to `kind == "probe"` before selecting. Two policy questions were required to be answered explicitly rather than left implicit in the implementation. **Both kinds failed** -> report the probe failure, since this function exists specifically to explain why zero probes are visible; the UART failure is real but a separate problem, and `hook_diagnostics` still carries every execution of both kinds verbatim. **No probe-kind failures** (including a UART-only failure with zero probe hooks) -> fall through to `no_native_probe_failure`, treated identically to "no hook data at all," because a UART hook's failure is not evidence about the probe side and must not be promoted into the probe overview's `code`/`agent_prompt`. Tests: `NoNativeProbeOverviewHookKindTests` in `tests/test_setup_overview_no_probe.py`, one per case, driving `_setup_overview` with a hand-built `InventorySnapshot` (the existing harness cannot carry hook diagnostics). **The implementer caught and corrected its own false green mid-work**: an earlier draft gave both hooks the same failure code and passed even with the bug reverted, so the final version gives them different codes (timeout vs nonzero-exit) to prove *which* execution was selected rather than merely that some failure was reported -- the same defect shape as C15/D16/D18, self-detected. Both tests verified to fail on reverting the filter. Suite 669. |
| M7 | (Found by the implementer while writing the D25 handler test, and disclosed rather than folded in silently.) Both `except UnsupportedProvider` handlers flattened the failure to `f"{failure.code}: {failure.message}"`, **silently dropping `failure.remedies`** -- so the actionable part of the payload, the instruction to install or enable a pyOCD probe plug-in, never reached the agent at all. This made the coordinator's own instruction ("assert the surfaced message carries the plug-in remedy") untestable as the code stood. | **MUST-FIX** | Discovered as a direct consequence of demanding a test for the D25 handlers -- the handler gap and the dropped-remedies gap were invisible to each other until something tried to assert on the surfaced text. This is the second time in this task that requiring a real test exposed a defect the fix itself had not (cf. D18). Fixing it is squarely within the D25 instruction rather than scope creep: the whole point of the typed payload is that its guidance reaches the caller. | Fixed: both handlers now append `' '.join(failure.remedies)`. Same `unsupported_provider_failure()` payload, no reshaping -- it simply stops discarding a field when flattening to text. |
| M8 | The two `server.py` `except UnsupportedProvider` handlers added by the D25 fix had no regression test; deleting either failed nothing. Recorded as a known residual gap in D25's own row at commit time and carried into the next dispatch rather than left to be rediscovered. | **MUST-FIX** | The identical situation as D20 -- a correct fix shipped without a guard -- and, more pointedly, the same "tested in isolation, never proven wired" mistake that produced this entire D25-D28 cluster, occurring one level up inside the fix for it. Worth recording separately from D25 because the pattern recurring *inside its own remedy* is the finding. | Fixed: `test_normal_connect_reports_an_unsupported_provider_through_the_real_handler` in `tests/test_server_assignment_connect.py`, following the existing `SelectionDisappeared` precedent (`test_normal_connect_rejects_missing_assigned_probe_without_fallback`) which drives `server._connect_impl(...)` end to end. Proven load-bearing by deleting the `except UnsupportedProvider` block and observing the failure, then restoring it. |
| D27 | `uart/open-failed` never fired anywhere. `open_failure_payload`'s single call site hardcoded `PROBE_OPEN_FAILED`, and UART write/read/exchange had **no exception handling around port I/O at all**, so a serial port failing mid-operation (cable pulled, device reset, port revoked by the OS) propagated a raw exception instead of the structured payload the guide specifies. | **MUST-FIX** (MEDIUM) | Last of the D25-D28 cluster, same root cause. Briefed more conservatively than the other three because it is the serial hot path and this subsystem has already produced one timeout-interaction defect (M6). | Fixed in `tools/serial.py` only. Catches **`RuntimeError` alone**, at exactly the three port I/O call sites -- not a bare `except Exception`, which would itself have been a defect here. Justified rather than assumed: `services/uart_capture.py` already wraps every I/O-section exception into `RuntimeError(...) from exc`, so `RuntimeError` is the only type a genuine port failure can surface as by the time it reaches this layer; parameter-validation `ValueError`s are raised earlier and are untouched. **Cancellation is explicitly protected**: `OperationCancelledError` is itself a `RuntimeError` subclass *and* is already collapsed into a generic `RuntimeError` by `uart_capture.py` before this layer sees it, surviving only as `exc.__cause__` -- so `_is_cancellation()` checks both the direct type and `__cause__`, and re-raises unchanged before any conversion. The hardcoded `PROBE_OPEN_FAILED` site was investigated and confirmed genuinely probe-only (`_connect_impl` only ever calls `open_session`, never a UART path), so it was left alone rather than made context-selecting. §8's structural guarantee that open-failure payloads cannot carry `hook_contract_call` untouched and re-verified through the real path. Six tests in `UartOpenFailureTests` drive the three tools with a fault-injecting `SerialToolServices` rather than the constructor in isolation, and all six proven load-bearing by reverting the try/except (genuine-failure test fails) and by forcing `_is_cancellation` to `False` (both cancellation tests fail). Suite 675. |
| M9 | (Found by the implementer while fixing D27, disclosed rather than silently fixed or silently ignored.) `services/uart_capture.py`'s pre-existing bare `except Exception as exc: raise RuntimeError(...) from exc` (lines 161, 209, 310) catches `OperationCancelledError` -- a `RuntimeError` subclass -- and destroys its type identity before it leaves the module. `kernel/operations.py:835`'s `except OperationCancelledError` therefore **can never match** for any UART operation, so cancellation falls through to `except BaseException` at :838 and the operation is recorded as **`FAILED` instead of `CANCELLED`**. | **EXTRANEOUS by scope, but a genuine defect -- flagged for triage, not dismissed** | Verified independently by the coordinator by reading all three wrap sites and both handlers. This is a real user-visible correctness bug: cancelling a long `read_serial` reports failure rather than cancellation. It is ruled extraneous *for this task* strictly because it is pre-existing and outside the charted diff, the same treatment given the `continue_setup` budget gap -- not because it is unimportant. D27 neither causes nor worsens it, and correctly avoids the worse outcome of relabelling cancellation as `uart/open-failed`. | **Not fixed, by decision, and recommended as the top follow-up.** Unlike the `continue_setup` gap, this one needs no design decision -- the fix is to re-raise `OperationCancelledError` unchanged ahead of the generic wrap at the three sites. It is deliberately not folded into D27 because that change sits in the serial hot path under a "change nothing else" instruction, and because scope discipline is what has kept this cluster's four fixes independently verifiable. |

## Notes

- D2 and C5 are marked "resolved as a consequence" per the implementer's directive: no
  code change was made specifically for them; they were closed by the FIX 1 and FIX 3(a)
  changes respectively, and are covered by those fixes' regression tests.
- D3 and D5 are the two findings ruled INVALID; both are left untouched in the code, as
  directed. D3's provenance clause was corrected during the D14/M4/M5 pass (its verdict
  and append-vs-replace reasoning are unchanged) after confirming `preflight.py` carries
  no uncommitted changes -- the sentence it describes is committed history, not a
  lingering stray edit.
- C15's disposition was a coordinator override, not the reviewer's own recommendation:
  the code adversary flagged it as informational/out-of-scope because the file predates
  this feature entirely. The coordinator directed a fix anyway, for the same reason C14
  was fixed rather than commented around -- the review's mandate covers the code as it
  stands, and a reproducible ~1-in-5 flake cannot underwrite a "green" status claim
  regardless of which commit introduced it.
- M4 and M5 are adoptions of another session's uncommitted/untracked work, not fixes
  produced by this feature's own implementation. They are recorded with that provenance
  explicit rather than folded silently into the rest of this ledger's fixes, per the
  same disclosure standard the rest of this document holds every other change to.
- FIX 8 required updating several existing tests' *expectations*, not just call sites,
  because the guide's step-0b "one canonical mint reproduces what all four former sites
  produced" invariant is necessarily superseded by provider-qualification (that is the
  fix). Specifically: `test_setup_overview_no_probe.py::ProbeConnectionIdTests` --
  `test_mint_is_stripped_and_casefolded` (asserted literal equality with the bare
  2-part legacy string) and `test_canonical_key_is_unchanged_versus_every_former_construction`
  (asserted `_setup_connection_key` produced the *same* key for the old and new mint)
  were rewritten to assert the new provider-qualified contract instead of the
  now-impossible old one; `test_stable_connection_identity_routes_through_the_shared_mint`'s
  mock metadata gained a `probe_family` field. Every other test in that class, plus
  mechanical call-site updates in `tests/test_probe_selection_records.py`,
  `tests/test_discovery_hook_workflow.py`, `tests/test_validation_honesty.py` (fixture
  handles needed a `probe_family`-bearing `board` so the provider check has something
  consistent to compare against), and `tests/test_server_trust_model_round_3.py` /
  `round_4.py` (hand-rolled legacy-shaped `connection_assignments` dicts updated to the
  canonical form `_setup_overview` now actually emits), only needed a provider argument
  or fixture field added -- their asserted *values* are unchanged.
- **Phase 1 terminated at its safety cap without ever clearing.** Iteration 5 was the last
  permitted iteration and produced two VALID findings (D15, D16), so the termination
  condition "a full iteration marks zero new findings VALID" was never met in five
  iterations. Per the standing directive, no iteration 6 was commissioned. The D15 and D16
  fixes recorded above were therefore applied *without* any adversarial pass over them --
  the ledger records this explicitly rather than letting "fixed" imply "reviewed." This is
  the primary blocker behind the RED status in `TASK_STATUS.md`, and it is not a
  bookkeeping technicality: two of this task's most severe findings (C12, D16) were each
  introduced by a *previous* fix and caught only by the following iteration, so unreviewed
  fixes are a known, demonstrated risk in this codebase, not a theoretical one.
- **A scoped review of D15/M6 was run after Phase 3, and was not a sixth iteration.**
  The safety cap forbids another full loop pass; it does not forbid reviewing a small
  body of production code that no adversary had ever seen. Scoped to
  `git diff 418b17d..459524b -- src/` plus its tests, it produced D17, D18, D19 --
  one real product-timeout gap and two tests that could not fail. It also returned
  substantive *negative* results, recorded here because they are findings too:
  `vendor_uart_rows` is correct; the coordinator's D15 design decision (wire through
  the parsers, not `resolve_serial_port`) was verified right; the `PermissionError`
  out-of-scope ruling holds, confirmed by reading all four `server.py` call sites and
  finding a broad `except Exception` at each; the `_DISCOVERY_HOOK_TOOLS` exclusion is
  correct; and the D16 fix works, reproducing the construction-timeout scenario 3/6
  under an adversarial tight loop and now surfacing it as a loud test ERROR rather
  than a silent pass.
- **A pre-existing, out-of-scope budget gap was found independently by both the
  reviewer and the implementer**, converging from opposite directions, and is
  deliberately NOT fixed: `continue_setup` (`server.py` `_setup_continue`) calls
  `_resolved_probe_uid_for_connection` -> `_hardware_inventory.snapshot()` directly,
  but is not a member of `_PROBE_INVENTORY_TOOLS`, so it receives only the flat
  default operation timeout with zero reservation for probe CLI, vendor CLI, *or*
  hooks. This is not a missing vendor term on an already-covered site -- it is a tool
  with no inventory budget at all, and it predates D15/M6 entirely. Fixing it requires
  a real categorization decision (should `continue_setup` join `_PROBE_INVENTORY_TOOLS`
  and always pay the larger budget, including on branches that never snapshot?), which
  is a design call outside this feature's scope. Flagged for separate triage.
- **A cross-agent revert destroyed in-flight work during the D15/D16 round**, and is
  recorded here because it affected what was verified rather than only how the work felt.
  The agent proving D16's test could fail was instructed to break `swd_process.py`
  temporarily and revert it; it reverted all of `src/`, wiping the concurrently-written
  D15 implementation in `hardware_inventory.py` and `server.py`. Detected by the
  coordinator diffing the tree rather than trusting the agent's "only `tests/` modified"
  report -- which was accurate about its own edits and wrong about the tree. D15 was
  re-applied identically. Root cause was coordinator scheduling (concurrent agents, one
  mutating the other's files) and a file-ownership instruction that was advisory rather
  than enforced; both agents now operate under an explicit prohibition on bulk
  `checkout`/`restore`/`stash` against paths they do not own. The break-and-verify
  technique itself was sound and is what proved D16's fix real rather than plausible.
