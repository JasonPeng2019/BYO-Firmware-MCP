Authorized local firmware validation. This is a host-only repository-quality follow-up in the
named local BYO-Firmware-MCP workspace. No board, hardware, remote system, or third-party target is
in scope.

Resume the persistent H05 marker-unlink **spec tester** role. The manager independently confirmed
that your Ruff formatting did take effect despite your last summary:

- `ruff format --check tests/test_h05_marker_unlink_spec.py` passes;
- `ruff check tests/test_h05_marker_unlink_spec.py` passes;
- the exact recorded spec command passes 5 tests;
- the manifest and recorded command remain unchanged.

Reread the complete `../.codex/design_charter.md`, verify those exact facts yourself using separate
PowerShell commands only (do not invoke `bash -lc` or compose shells), append the
missing dated **Spec tester — manager format pre-final follow-up** entry to
`.change-loop/fresh-suite/H05-marker-unlink/DESIGN_CHARTER_CHECKS.md`, reread the charter once more,
and report. The entry must state that the formatting is mechanical, assertions/semantics and the
command/manifest are preserved, all checks pass, and no production/hardware/unrelated change was
made.

Edit only the named charter checkpoint log. Do not reformat or edit any test or production file,
manifest, command, plan, request, or unrelated dirty file. Do not operate hardware. Do not commit.
