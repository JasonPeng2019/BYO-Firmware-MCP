Authorized local firmware-server validation. Resume the same review session.
The prior Windows workspace-write sandbox rejected the one explicitly required
review-artifact write, so this resume uses the documented
`--sandbox danger-full-access` fallback without bypassing approvals. The user
and main orchestrator authorize exactly the previously requested local write.

Your technical review is complete, but the task is not complete until the
artifact exists. Read `../.codex/design_charter.md` once more, then write
exactly:

`.change-loop/fresh-suite/H04-pack-repair-zero-retries/plan-review.md`

Use your already completed analysis. Include:

- exact plain line
  `Plan SHA-256: 8bb7371fd01f2bc751a78a18fffd390f638ec434b9dbd99656ab448df4504da1`
- exact session identity `019f9c2e-6feb-7b61-a730-37320001bf49`
- model `gpt-5.6-terra`, medium reasoning
- verdict
- all three design-charter checkpoints
- numbered execution risks/test targets

Then reread that file and finish. Do not write or alter anything else.
