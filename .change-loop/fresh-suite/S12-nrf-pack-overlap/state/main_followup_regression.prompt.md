Authorized local firmware validation. This is a host-only adversarial regression test in the
named BYO-Firmware-MCP workspace. No hardware action is authorized or needed.

You are the same persistent change-loop regression tester. Main-model evidence review found that
the green S12 regression currently constructs `PackMemoryGeometry` directly and therefore does not
independently trace the real pack replay/adapter integration required by CL-002/CL-003.

Read the complete plan, plan review, latest neutral report, current source/diff, the spec tester's
new coverage for context, and the full `../.codex/design_charter.md`. Do not alter the spec
tester's files. Edit only your manifested S12 regression test to add complementary deterministic,
offline coverage through the actual `resolve_registered_pack_geometry` production function and
then the generic-map consumer boundary. Independently protect the adjacent contracts emphasized
by the plan and plan review: stable scalar default flash/RAM selection, deterministic replay,
preservation of disjoint banks, and no invented erase/application/write authority. Reuse existing
fixture mechanisms; no network, hardware, external SDK, or S12 experiment access.

Keep your recorded command portable with forward slashes, update only your manifest/command, and
run it from the repository root. Append all required dated design-charter checkpoints to the
runtime log before analysis, immediately before editing, between distinct test features, before
verification, and before the verdict. Do not edit production source, the spec tester's files, or
commit.
